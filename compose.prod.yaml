# Compose para deploy de Produção na VPS (Puxando Imagens do Docker Hub)
services:
  # 1. MICROSERVIÇO NODE.JS (IA SPEAKING & GEMINI KEY)
  backend:
    image: ${DOCKER_USER:-davs933}/ingles-proposito-backend:latest
    container_name: ingles_proposito_backend
    restart: always
    environment:
      - PORT=4000
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend.rule=Host(`ingles.techrocket.site`) && PathPrefix(`/api`)"
      - "traefik.http.routers.backend.priority=100"
      - "traefik.http.routers.backend.entrypoints=websecure"
      - "traefik.http.routers.backend.tls=true"
      - "traefik.http.routers.backend.tls.certresolver=lets-encrypt"
      - "traefik.http.services.backend.loadbalancer.server.port=4000"
    networks:
      - web

  # 2. FRONTEND PWA & DASHBOARD (REACT / NGINX)
  frontend:
    image: ${DOCKER_USER:-davs933}/ingles-proposito-frontend:latest
    container_name: ingles_proposito_frontend
    restart: always
    environment:
      - VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
      - VITE_SUPABASE_ANON_KEY=${VITE_SUPABASE_ANON_KEY}
      - VITE_BACKEND_URL=${VITE_BACKEND_URL}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.rule=Host(`ingles.techrocket.site`)"
      - "traefik.http.routers.frontend.priority=1"
      - "traefik.http.routers.frontend.entrypoints=websecure"
      - "traefik.http.routers.frontend.tls=true"
      - "traefik.http.routers.frontend.tls.certresolver=lets-encrypt"
      - "traefik.http.services.frontend.loadbalancer.server.port=80"
    networks:
      - web

networks:
  web:
    external: true
